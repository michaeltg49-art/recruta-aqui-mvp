const initial=[{id:1,title:'Analista de Recrutamento',company:'Horizonte Pessoas',place:'Remoto',type:'CLT',level:'Pleno',area:'Recursos Humanos',pay:'Faixa a combinar',desc:'Conduzir entrevistas, acompanhar indicadores e criar uma experiência acolhedora para candidatos.',skills:['Entrevistas','Organização','Comunicação']},{id:2,title:'Assistente Administrativo',company:'Grupo Norte',place:'Arapiraca, AL',type:'CLT',level:'Júnior',area:'Administração',pay:'R$ 2.100 – 2.600',desc:'Apoiar rotinas administrativas, organizar documentos e atender clientes com atenção aos detalhes.',skills:['Excel','Organização','Atendimento']},{id:3,title:'Analista de Dados',company:'Orbe Tecnologia',place:'Híbrido · Maceió, AL',type:'CLT',level:'Pleno',area:'Tecnologia',pay:'Faixa a combinar',desc:'Transformar dados em decisões, construir painéis e colaborar com diferentes áreas do negócio.',skills:['Excel','Power BI','Comunicação']},{id:4,title:'Consultor Comercial',company:'Viva Saúde',place:'Arapiraca, AL',type:'PJ',level:'Pleno',area:'Comercial',pay:'Fixo + comissão',desc:'Criar relacionamento com clientes, entender necessidades e acompanhar oportunidades.',skills:['Atendimento','Comunicação','Negociação']}];
const demoCandidates=[{name:'Mariana Costa',city:'Arapiraca, AL',area:'Recursos Humanos',skills:['Entrevistas','Organização','Comunicação'],experience:'3 anos em recrutamento',interest:'Pessoas e processos'},{name:'Lucas Almeida',city:'Maceió, AL',area:'Tecnologia',skills:['Excel','Power BI','Comunicação'],experience:'2 anos em análise de dados',interest:'Dados e indicadores'},{name:'Camila Ferreira',city:'Arapiraca, AL',area:'Administração',skills:['Excel','Organização','Atendimento'],experience:'2 anos em rotinas administrativas',interest:'Organização e atendimento'},{name:'Rafael Oliveira',city:'Arapiraca, AL',area:'Comercial',skills:['Negociação','Comunicação','Atendimento'],experience:'4 anos em vendas consultivas',interest:'Relacionamento com clientes'},{name:'Beatriz Santos',city:'Maceió, AL',area:'Recursos Humanos',skills:['Comunicação','Atendimento'],experience:'1 ano em atendimento',interest:'Transição para RH'}];let jobs=[...initial],page='home',role='candidate',profile=null,applications=[],appointments=[],questionIndex=0,answers=[],chosen=null,query='',activeJob=1,adminTab='overview',candidateTab='overview',photoUrl=null;const questions=[{q:'Qual área mais combina com seus interesses?',a:['Recursos Humanos','Administração','Tecnologia','Comercial'],kind:'area'},{q:'Qual habilidade você já utiliza no trabalho ou estudo?',a:['Comunicação','Excel','Power BI','Negociação'],kind:'skill'},{q:'Duas tarefas urgentes chegam ao mesmo tempo. O que você faria primeiro?',a:['Confirmaria prioridades e prazos com quem solicitou','Organizaria as etapas e comunicaria um plano','Pediria apoio para dividir as tarefas'],kind:'Organização'},{q:'Um colega discorda da sua proposta. Como você conduziria a conversa?',a:['Perguntaria quais pontos ele vê de outra forma','Apresentaria evidências e ouviria a resposta','Buscaria uma alternativa que atendesse aos objetivos'],kind:'Colaboração'},{q:'Uma informação importante para a equipe mudou. Qual seria sua primeira ação?',a:['Avisaria as pessoas afetadas com clareza','Confirmaria os detalhes antes de comunicar','Atualizaria os registros e alinharia os próximos passos'],kind:'Comunicação'},{q:'Você recebe uma tarefa nova, sem instruções completas. Como começaria?',a:['Perguntaria qual é o resultado esperado','Buscaria exemplos e organizaria um primeiro rascunho','Proporia uma primeira abordagem para receber retorno'],kind:'Aprendizagem'},{q:'Você percebe um erro em uma entrega ainda não enviada. O que faria?',a:['Corrigiria e verificaria o impacto nas outras partes','Comunicaria o erro e pediria uma segunda revisão','Registraria o aprendizado para evitar recorrência'],kind:'Responsabilidade'},{q:'O prazo de uma atividade mudou inesperadamente. Como reagiria?',a:['Replanejaria as etapas e avisaria sobre os impactos','Alinharia o que é essencial entregar primeiro','Pediria colaboração para preservar a qualidade'],kind:'Adaptação'}];const $=()=>document.getElementById('app');const escapeHTML=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function nav(p){page=p;window.scrollTo(0,0);render()}function setRole(r){role=r;nav(r==='admin'?'admin':'candidate')}function toggleMenu(){const menu=document.getElementById('main-nav'),button=document.getElementById('menu-toggle');const open=menu.classList.toggle('open');button.setAttribute('aria-expanded',String(open));button.textContent=open?'Fechar':'Menu'}function header(){return `<header class="top"><a class="brand" href="#" onclick="nav('home');return false"><i>R</i>recruta aqui<span style="color:#706afa">.</span></a><button id="menu-toggle" class="mobile-menu" type="button" aria-controls="main-nav" aria-expanded="false" onclick="toggleMenu()">Menu</button><nav id="main-nav" class="nav" aria-label="Navegação principal"><button class="${page==='home'?'active':''}" onclick="nav('home')">Início</button><button class="${page==='jobs'?'active':''}" onclick="nav('jobs')">Encontrar vagas</button><button class="${page==='candidate'?'active':''}" onclick="setRole('candidate')">Área do candidato</button><button class="${page==='admin'?'active':''}" onclick="setRole('admin')">Para recrutadores</button><button class="mobile-login" onclick="nav('login')">Entrar</button></nav><div class="top-actions"><button class="pill" onclick="nav('login')">Entrar</button><button class="primary" onclick="nav('register')">Criar conta</button></div></header>`}function footer(){return `<footer class="footer"><span>© 2026 Recruta Aqui · Demonstração de produto</span><span>Vagas e empresas ilustrativas</span></footer>`}function cards(list){return list.length?`<div class="grid">${list.map(j=>`<article class="card"><div class="company"><span class="avatar">${escapeHTML(j.company[0])}</span><div><b>${escapeHTML(j.company)}</b><br><small>${escapeHTML(j.place)}</small></div></div><h3>${escapeHTML(j.title)}</h3><div class="tags"><span class="tag">${escapeHTML(j.type)}</span><span class="tag">${escapeHTML(j.level)}</span><span class="tag">${escapeHTML(j.area)}</span></div><div class="card-foot"><span>${escapeHTML(j.pay)}</span><button class="ghost" onclick="activeJob=${j.id};nav('job')">Ver vaga →</button></div></article>`).join('')}</div>`:`<div class="panel">Nenhuma vaga encontrada. Tente outro termo de busca.</div>`}function home(){return `<section class="hero"><div class="hero-copy"><div class="eyebrow" style="color:#b8b5ff">Carreira com mais direção</div><h1>Pessoas certas. <em>Resultados reais.</em></h1><p>Recrutamento e seleção com clareza, método e agilidade. Conectamos empresas a talentos com o perfil certo, no tempo certo.</p><button class="primary" onclick="nav('jobs')">Explorar vagas ↗</button></div><div class="hero-art"><div class="orbit"><div class="float"><small>Conexão com oportunidades</small><b>Conexões com critério</b><div class="meter"><span></span></div></div></div></div></section><div class="section-head"><div><div class="eyebrow">Oportunidades abertas</div><h2>Vagas para explorar</h2><span class="muted">Encontre um caminho que combine com você.</span></div><button class="pill" onclick="nav('jobs')">Ver todas →</button></div>${cards(jobs.slice(0,3))}<section class="story"><div><span class="eyebrow">Quem somos</span><h2>Contratar bem é uma decisão estratégica.</h2></div><div><p>A Recruta Aqui nasceu para simplificar o recrutamento e tornar as decisões mais seguras. Mais do que currículos, entregamos análise, critérios claros e alinhamento entre pessoas e empresas.</p><p>Reduzimos o retrabalho de processos confusos e damos visibilidade às etapas, para que empresas foquem no crescimento do negócio e candidatos saibam onde estão.</p></div></section>`}
function jobsPage(){let filtered=jobs.filter(j=>(j.title+' '+j.company+' '+j.place+' '+j.area).toLowerCase().includes(query.toLowerCase()));return `<div class="eyebrow">Oportunidades</div><h1 class="page-title">Encontre sua próxima vaga</h1><p class="muted">Pesquise por cargo, empresa, área ou cidade.</p><div class="searchbar"><input aria-label="Buscar vagas" placeholder="Ex.: Analista, Arapiraca, tecnologia" value="${escapeHTML(query)}" oninput="query=this.value;document.getElementById('results').innerHTML=renderResults()"><button class="primary" onclick="document.querySelector('.searchbar input').focus()">Buscar vagas</button></div><div id="results">${renderResults()}</div>`}function renderResults(){let l=jobs.filter(j=>(j.title+' '+j.company+' '+j.place+' '+j.area).toLowerCase().includes(query.toLowerCase()));return `<div class="section-head"><h2>${l.length} vaga${l.length===1?'':'s'} encontrada${l.length===1?'':'s'}</h2></div>${cards(l)}`}
function jobPage(){let j=jobs.find(x=>x.id===activeJob)||jobs[0];return `<button class="ghost" onclick="nav('jobs')">← Todas as vagas</button><div class="detail panel" style="margin-top:22px"><div class="company"><span class="avatar">${escapeHTML(j.company[0])}</span><div><b>${escapeHTML(j.company)}</b><br><small>${escapeHTML(j.place)}</small></div></div><h1 class="page-title">${escapeHTML(j.title)}</h1><div class="tags"><span class="tag">${escapeHTML(j.type)}</span><span class="tag">${escapeHTML(j.level)}</span><span class="tag">${escapeHTML(j.area)}</span><span class="tag">${escapeHTML(j.pay)}</span></div><h3>Sobre a oportunidade</h3><p>${escapeHTML(j.desc)}</p><h3>Habilidades relevantes</h3><div class="tags">${j.skills.map(s=>`<span class="tag">${escapeHTML(s)}</span>`).join('')}</div><p class="muted">Sua candidatura será analisada por uma pessoa. O perfil pode ajudar a organizar as vagas sugeridas.</p><button class="primary" onclick="apply(${j.id})">Candidatar-se</button></div>`}function apply(id){if(!profile){activeJob=id;nav('register');return}if(!applications.includes(id))applications.push(id);candidateTab='applications';nav('candidate')}
function register(){return `<div class="form panel"><div class="eyebrow">Conta do candidato</div><h1 class="page-title">Crie seu perfil</h1><p class="muted">Preencha seus dados para experimentar a jornada do candidato.</p><div class="notice">Demonstração: os dados desta tela ficam apenas durante esta visita. Não use uma senha pessoal ou dados sensíveis.</div><form onsubmit="saveProfile(event)"><label class="field">Nome completo<input name="name" required placeholder="Seu nome"></label><label class="field">E-mail<input type="email" name="email" required placeholder="voce@exemplo.com"></label><label class="field">Foto de perfil opcional<input type="file" name="photo" accept="image/jpeg,image/png,image/webp" aria-describedby="photo-help"><small id="photo-help" class="muted">JPG, PNG ou WebP; até 2 MB. A foto não influencia sugestões de vagas.</small></label><label class="field">Senha de demonstração<input type="password" name="password" required minlength="8" autocomplete="new-password" placeholder="Mínimo de 8 caracteres"></label><button class="primary">Continuar para o perfil →</button></form><p class="muted">Já começou? <a href="#" onclick="nav('login');return false">Entrar na demonstração</a></p></div>`}function saveProfile(e){e.preventDefault();let f=new FormData(e.target);profile={name:String(f.get('name')),email:String(f.get('email'))};if(!setPhoto(f.get('photo')))return;questionIndex=0;answers=[];nav('questionnaire')}
function setPhoto(file){if(!file||!file.size)return true;if(!['image/jpeg','image/png','image/webp'].includes(file.type)||file.size>2*1024*1024){alert('Escolha uma foto JPG, PNG ou WebP de até 2 MB.');return false}if(photoUrl)URL.revokeObjectURL(photoUrl);photoUrl=URL.createObjectURL(file);return true}function updatePhoto(e){if(setPhoto(e.target.files[0]))render()}function removePhoto(){if(photoUrl)URL.revokeObjectURL(photoUrl);photoUrl=null;render()}function login(){return `<div class="form panel"><div class="eyebrow">Acesso de demonstração</div><h1 class="page-title">Bem-vindo de volta</h1><div class="notice">Nesta versão, ainda não há autenticação. Para conhecer o painel, escolha uma das experiências abaixo.</div><div class="two" style="margin-top:22px"><button class="primary" onclick="profile=profile||{name:'Candidato de demonstração',email:'demo@exemplo.com'};setRole('candidate')">Ver área do candidato</button><button class="pill" onclick="setRole('admin')">Ver área da recrutadora</button></div></div>`}
function questionnaire(){let q=questions[questionIndex];if(!q)return candidate();return `<div class="form panel"><div class="eyebrow">Mapeamento de perfil · ${questionIndex+1} de ${questions.length}</div><h1 class="page-title">Como você lida com situações de trabalho?</h1><p class="muted">Escolha a alternativa mais próxima do que faria. Não existem respostas certas ou erradas. Este questionário não é um teste psicológico.</p><div class="steps">${questions.map((_,i)=>`<span class="step ${i<=questionIndex?'on':''}">${i+1}</span>`).join('')}</div><h2 class="question">${q.q}</h2><div class="choices">${q.a.map((a,i)=>`<button class="choice ${chosen===i?'selected':''}" onclick="chosen=${i};render()">${a}</button>`).join('')}</div><button class="primary" onclick="nextQuestion()" ${chosen===null?'disabled':''}>${questionIndex===questions.length-1?'Ver meu perfil':'Continuar →'}</button></div>`}function nextQuestion(){if(chosen===null)return;answers[questionIndex]=questions[questionIndex].a[chosen];chosen=null;questionIndex++;nav(questionIndex===questions.length?'candidate':'questionnaire')}
function match(j){if(answers.length<2)return null;let score=45,reasons=[];if(answers[0]===j.area){score+=30;reasons.push('área de interesse')}if(j.skills.includes(answers[1])){score+=20;reasons.push('habilidade informada')}return {score,reasons}}function candidate(){let name=profile?.name?.split(' ')[0]||'Visitante';return `<div class="layout"><aside class="side">${[['overview','Visão geral'],['profile','Meu perfil'],['matches','Vagas sugeridas'],['applications','Candidaturas'],['interviews','Entrevistas']].map(([k,v])=>`<button class="${candidateTab===k?'active':''}" onclick="candidateTab='${k}';render()">${v}</button>`).join('')}</aside><div><div class="eyebrow">Espaço do candidato</div><div class="candidate-heading">${photoUrl?`<img class="profile-photo" src="${photoUrl}" alt="Foto de perfil">`:`<span class="profile-photo placeholder" aria-hidden="true">${escapeHTML(name[0])}</span>`}<h1 class="page-title">Olá, ${escapeHTML(name)} 👋</h1></div>${candidateContent()}</div></div>`}function candidateContent(){if(candidateTab==='overview')return `<div class="statgrid"><div class="panel stat"><span>Candidaturas</span><strong>${applications.length}</strong></div><div class="panel stat"><span>Entrevistas</span><strong>${appointments.length}</strong></div><div class="panel stat"><span>Perfil</span><strong>${answers.length===questions.length?'Pronto':'Pendente'}</strong></div></div><div class="two"><div class="panel"><div class="eyebrow">Próximo passo</div><h2>Conte mais sobre você</h2><p class="muted">Seu perfil ajuda a destacar vagas de interesse.</p><button class="primary" onclick="questionIndex=0;chosen=null;nav('questionnaire')">${answers.length===questions.length?'Refazer questionário':'Responder questionário'}</button></div><div class="panel"><div class="eyebrow">Seu processo</div><h2>Acompanhe cada etapa</h2><p class="muted">Veja suas candidaturas e futuras entrevistas em um lugar só.</p><button class="pill" onclick="candidateTab='applications';render()">Ver candidaturas →</button></div></div>`;if(candidateTab==='profile')return `<div class="two"><div class="panel"><h2>Meu perfil</h2><p><b>Nome:</b> ${escapeHTML(profile?.name||'Candidato de demonstração')}</p><p><b>E-mail:</b> ${escapeHTML(profile?.email||'Não informado')}</p><label class="field">Foto de perfil (opcional)<input type="file" accept="image/jpeg,image/png,image/webp" onchange="updatePhoto(event)"><small class="muted">JPG, PNG ou WebP; até 2 MB. Fica apenas nesta visita.</small></label>${photoUrl?'<button class="pill" onclick="removePhoto()">Remover foto</button>':''}</div><div class="panel"><h2>Situações de trabalho</h2>${answers.length===questions.length?`<p class="muted">Suas escolhas descrevem formas de agir em situações comuns. Elas não medem personalidade, aptidão ou saúde mental.</p>${questions.slice(2).map((q,i)=>`<div class="response-row"><b>${q.kind}</b><span>${escapeHTML(answers[i+2])}</span></div>`).join('')}`:'<p class="muted">Responda ao questionário para ver suas escolhas aqui.</p>'}<button class="pill" onclick="questionIndex=0;chosen=null;nav('questionnaire')">${answers.length===questions.length?'Refazer':'Responder'} questionário</button></div></div>`;if(candidateTab==='matches')return `<div class="notice">Sugestões demonstrativas baseadas somente na área e na habilidade informadas. A foto e as respostas às situações de trabalho não influenciam a ordem ou a pontuação. Não há avaliação por IA nem decisão automática.</div><div class="section-head"><h2>Vagas sugeridas</h2></div>${answers.length!==questions.length?`<div class="panel">Responda o questionário para ver sugestões. <button class="pill" onclick="questionIndex=0;nav('questionnaire')">Começar</button></div>`:jobs.map(j=>{let m=match(j);return `<div class="panel" style="margin-bottom:12px"><div class="row"><div><h3>${escapeHTML(j.title)}</h3><p>${escapeHTML(j.company)} · ${escapeHTML(j.place)}</p><small class="muted">${m.reasons.length?'Por quê: '+m.reasons.join(' e '):'Sugestão geral; poucos critérios em comum'}</small></div><div><span class="badge">${m.score}% indicativo</span><button class="ghost" onclick="activeJob=${j.id};nav('job')">Ver vaga →</button></div></div></div>`}).join('')}`;if(candidateTab==='applications')return `<div class="panel"><h2>Minhas candidaturas</h2>${applications.length?applications.map(id=>{let j=jobs.find(x=>x.id===id);return j?`<div class="row"><div><h3>${escapeHTML(j.title)}</h3><p>${escapeHTML(j.company)}</p></div><span class="badge amber">Inscrição recebida</span></div>`:''}).join(''):'<p class="muted">Você ainda não se candidatou. Explore as vagas para começar.</p>'}<button class="pill" onclick="nav('jobs')">Explorar vagas</button></div>`;return `<div class="panel"><h2>Entrevistas</h2>${appointments.length?appointments.map(a=>`<div class="row"><div><h3>${escapeHTML(a.title)}</h3><p>${escapeHTML(a.date)} · ${escapeHTML(a.time)}</p></div><span class="badge green">Agendada</span></div>`).join(''):'<p class="muted">Nenhuma entrevista agendada nesta demonstração.</p>'}</div>`}
function admin(){return `<div class="layout"><aside class="side">${[['overview','Visão geral'],['vacancies','Vagas'],['create','Publicar vaga'],['agenda','Agenda'],['matching','Cruzamento'],['pipeline','Candidaturas']].map(([k,v])=>`<button class="${adminTab===k?'active':''}" onclick="adminTab='${k}';render()">${v}</button>`).join('')}</aside><div><div class="eyebrow">Painel da recrutadora · Demonstração</div><h1 class="page-title">Gerencie seus processos</h1>${adminContent()}</div></div>`}function adminContent(){if(adminTab==='overview')return `<div class="statgrid"><div class="panel stat"><span>Vagas no catálogo</span><strong>${jobs.length}</strong></div><div class="panel stat"><span>Inscrições nesta visita</span><strong>${applications.length}</strong></div><div class="panel stat"><span>Entrevistas nesta visita</span><strong>${appointments.length}</strong></div></div><div class="two"><div class="panel"><h2>Publique uma oportunidade</h2><p class="muted">Defina cargo, modalidade, localização e requisitos para apresentar a vaga.</p><button class="primary" onclick="adminTab='create';render()">Criar vaga →</button></div><div class="panel"><h2>Organize as entrevistas</h2><p class="muted">Registre data e horário para visualizar os próximos encontros.</p><button class="pill" onclick="adminTab='agenda';render()">Abrir agenda →</button></div></div>`;if(adminTab==='vacancies')return `<div class="section-head"><h2>Vagas publicadas</h2><button class="primary" onclick="adminTab='create';render()">+ Nova vaga</button></div>${jobs.map(j=>`<div class="panel row" style="margin-bottom:10px"><div><h3>${escapeHTML(j.title)}</h3><p>${escapeHTML(j.company)} · ${escapeHTML(j.place)}</p></div><span class="badge green">Visível</span></div>`).join('')}`;if(adminTab==='create')return `<div class="panel form"><h2>Nova oportunidade</h2><p class="notice">Vagas criadas aqui aparecem nesta visita. Publicação permanente virá com o backend.</p><form onsubmit="addJob(event)"><label class="field">Título da vaga<input name="title" required placeholder="Ex.: Analista Financeiro"></label><label class="field">Empresa<input name="company" required placeholder="Nome da empresa"></label><label class="field">Localização<input name="place" required placeholder="Ex.: Arapiraca, AL ou Remoto"></label><div class="two"><label class="field">Contratação<select name="type"><option>CLT</option><option>PJ</option><option>Estágio</option></select></label><label class="field">Senioridade<select name="level"><option>Júnior</option><option>Pleno</option><option>Sênior</option></select></label></div><label class="field">Área<select name="area"><option>Administração</option><option>Recursos Humanos</option><option>Tecnologia</option><option>Comercial</option></select></label><label class="field">Faixa de remuneração<input name="pay" placeholder="Ex.: R$ 2.000 – 3.000"></label><label class="field">Descrição<textarea name="desc" required placeholder="Responsabilidades e contexto da vaga"></textarea></label><label class="field">Habilidades (separe por vírgulas)<input name="skills" placeholder="Excel, Comunicação, Organização"></label><button class="primary">Publicar na demonstração</button></form></div>`;if(adminTab==='matching')return `<div class="notice">Pessoas e vagas abaixo são fictícias. A pontuação usa apenas área de interesse e habilidades declaradas, sem IA nem decisão automática de contratação. A recrutadora deve avaliar cada pessoa.</div><div class="section-head"><div><h2>Conexões com critério</h2><span class="muted">Compare perfis e entenda o motivo de cada sugestão.</span></div><select aria-label="Selecionar vaga para comparação" class="job-select" onchange="activeJob=Number(this.value);render()">${jobs.map(j=>`<option value="${j.id}" ${activeJob===j.id?'selected':''}>${escapeHTML(j.title)}</option>`).join('')}</select></div>${demoCandidates.map(c=>{let j=jobs.find(x=>x.id===activeJob)||jobs[0];let common=c.skills.filter(x=>j.skills.includes(x));let area=c.area===j.area;let score=35+(area?30:0)+Math.min(common.length,3)*10;return `<article class="panel match-card"><div class="person-icon">${escapeHTML(c.name.split(' ').map(x=>x[0]).join(''))}</div><div class="match-body"><h3>${escapeHTML(c.name)}</h3><p class="muted">${escapeHTML(c.city)} · ${escapeHTML(c.experience)}</p><div class="tags"><span class="tag">${escapeHTML(c.area)}</span>${c.skills.map(x=>`<span class="tag">${escapeHTML(x)}</span>`).join('')}</div><p class="why"><b>Critérios:</b> ${area?'área de interesse alinhada':'área de interesse diferente'}; ${common.length?common.map(escapeHTML).join(', ')+' em comum':'nenhuma habilidade informada em comum'}.</p></div><div class="match-score"><b>${score}%</b><small>indicativo</small></div></article>`}).sort((a,b)=>Number((b.match(/<b>(\d+)%<\/b>/)||[])[1])-Number((a.match(/<b>(\d+)%<\/b>/)||[])[1])).join('')}`;if(adminTab==='agenda')return `<div class="two"><div class="panel"><h2>Agendar entrevista</h2><form onsubmit="addAppointment(event)"><label class="field">Vaga<select name="title">${jobs.map(j=>`<option>${escapeHTML(j.title)}</option>`).join('')}</select></label><label class="field">Data<input name="date" type="date" required></label><label class="field">Horário<input name="time" type="time" required></label><button class="primary">Adicionar à agenda</button></form></div><div class="panel"><h2>Próximas entrevistas</h2>${appointments.length?appointments.map(a=>`<div class="row"><div><h3>${escapeHTML(a.title)}</h3><p>${escapeHTML(a.date)} · ${escapeHTML(a.time)}</p></div><span class="badge green">Agendada</span></div>`).join(''):'<p class="muted">Nenhuma entrevista registrada nesta visita.</p>'}</div></div>`;return `<div class="panel"><h2>Pipeline de candidaturas</h2><p class="notice">Nesta demonstração, aparecem apenas candidaturas feitas neste mesmo navegador e nesta visita.</p>${applications.length?applications.map(id=>{let j=jobs.find(x=>x.id===id);return j?`<div class="row"><div><h3>${escapeHTML(profile?.name||'Candidato de demonstração')}</h3><p>${escapeHTML(j.title)}</p></div><span class="badge amber">Inscrição recebida</span></div>`:''}).join(''):'<p class="muted">Ainda não há candidaturas nesta visita.</p>'}</div>`}function addJob(e){e.preventDefault();let f=new FormData(e.target);jobs.unshift({id:Date.now(),title:f.get('title'),company:f.get('company'),place:f.get('place'),type:f.get('type'),level:f.get('level'),area:f.get('area'),pay:f.get('pay')||'Faixa a combinar',desc:f.get('desc'),skills:String(f.get('skills')).split(',').map(s=>s.trim()).filter(Boolean)});adminTab='vacancies';render()}function addAppointment(e){e.preventDefault();let f=new FormData(e.target);appointments.push({title:f.get('title'),date:f.get('date'),time:f.get('time')});render()}
function render(){let content=({home,jobs:jobsPage,job:jobPage,register,login,questionnaire,candidate,admin})[page]();$().innerHTML=`<div class="shell">${header()}<main class="main">${content}</main>${footer()}</div>`}render();
